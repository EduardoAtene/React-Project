import styled from 'styled-components'

export const TutorialContainer = styled.div`
  h2 {
    color: #007bff;
    margin: 0;
  }
  
  h4 {
    padding-top: 20px;
    color: #007bff;
  }
  
  p {
    color: black;
  }
  color: black;
  background-color: white;
  border-radius: 6px;
  padding: 25px;
  height: 90%;
  margin: 40px;
  padding: 50px;
  box-shadow: 1%;
  box-shadow: 0px 0px 15px 1px black;

`
export const ImagemAnuncio  = styled.div`
  display: inline-block; /* Exibe os cards um ao lado do outro */
  width: calc(50% - 16px); /* Define a largura dos cards (25% - margem) */
  margin: 8px; /* Adiciona uma margem entre os cards */
`

export const TextoAnuncio  = styled.div`
  display: inline-block; /* Exibe os cards um ao lado do outro */
  width: calc(50% - 16px); /* Define a largura dos cards (25% - margem) */
  margin: 8px; /* Adiciona uma margem entre os cards */
`